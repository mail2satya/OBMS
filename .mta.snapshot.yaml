_schema-version: '3.1'
ID: OBMS
version: 1.0.0
description: A simple CAP project.
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
  subdomain: 4e56ed52trial
build-parameters:
  before-all:
    - builder: custom
      commands:
        - rm -rf build/dist
        - mkdir -p build/dist
        - rm -rf build/ui-deployer
        - mkdir -p build/ui-deployer
    - builder: custom
      commands:
        - npm install --production
        - >-
          bash -c "cds -v | grep 'cds-dk' || npm i --production --no-save
          @sap/cds-dk"
        - bash -c "export PATH=$PATH:$(pwd)/node_modules/.bin"
        - cds build --production
  after-all:
    - builder: custom
      commands:
        - npm install
modules:
  - name: OBMS-customermanagement
    type: html5
    path: app/customer-management
    build-parameters:
      builder: custom
      commands:
        - rm -rf dist-zip dist
        - npm install
        - >-
          npx -p @ui5/cli@^3.0.0 ui5 build --dest dist
          --include-task=generateCachebusterInfo
        - mkdir dist-zip
        - bash -c "cd dist && npx bestzip ../dist-zip/customermanagement.zip *"
      _artifactName: customermanagement.zip
      supported-platforms: []
      build-result: dist-zip
  - name: OBMS-db-deployer
    type: hdb
    path: gen/db
    parameters:
      buildpack: nodejs_buildpack
    build-parameters:
      builder: npm
      ignore:
        - node_modules
        - package-lock.json
    requires:
      - name: OBMS-service-uaa
      - name: OBMS-service-db
  - name: OBMS-srv
    type: nodejs
    path: gen/srv
    parameters:
      memory: 256MB
    provides:
      - name: srv-api
        properties:
          srv-url: ${default-url}
    build-parameters:
      builder: npm
      ignore:
        - node_modules
        - package-lock.json
    requires:
      - name: OBMS-service-uaa
      - name: OBMS-service-db
      - name: OBMS-service-destination
      - name: OBMS-service-metadata
      - name: OBMS-service-connectivity
      - name: OBMS-service-logs
  - name: OBMS-aux-destinations
    type: com.sap.application.content
    parameters:
      content:
        subaccount:
          existing_destinations_policy: update
          destinations:
            - Authentication: OAuth2UserTokenExchange
              Name: OBMS-service-uaa
              ServiceInstanceName: OBMS-uaa
              ServiceKeyName: OBMS-uaa-key
              sap.cloud.service: lcap.OBMS
            - Authentication: OAuth2UserTokenExchange
              Name: OBMS-app-srv
              TokenServiceInstanceName: OBMS-uaa
              TokenServiceKeyName: OBMS-uaa-key
              URL: ~{srv-api/srv-url}
              sap.cloud.service: lcap.OBMS
            - Name: OBMS-service-html5-apps-repo-host
              ServiceInstanceName: OBMS-html5-apps-repo-host
              ServiceKeyName: OBMS-html5-apps-repo-host-key
              sap.cloud.service: lcap.OBMS
            - Authentication: OAuth2UserTokenExchange
              Name: OBMS-oBMSSrv
              TokenServiceInstanceName: OBMS-uaa
              TokenServiceKeyName: OBMS-uaa-key
              URL: ~{srv-api/srv-url}/service/oBMS
              sap.cloud.service: lcap.OBMS
              sap.applicationdevelopment.actions.enabled: false
              sap.build.usage: CAP
              sap.processautomation.enabled: false
              HTML5.DynamicDestination: false
              WebIDEEnabled: false
              MobileEnabled: false
              AppgyverEnabled: false
    build-parameters:
      no-source: true
    requires:
      - name: OBMS-service-uaa
        parameters:
          service-key:
            name: OBMS-uaa-key
      - name: srv-api
      - name: OBMS-service-html5-apps-repo-host
        parameters:
          service-key:
            name: OBMS-html5-apps-repo-host-key
      - name: OBMS-service-destination
        parameters:
          content-target: true
  - name: OBMS-launchpad
    type: html5
    path: build/launchpad
    build-parameters:
      builder: custom
      commands:
        - npm install
        - >-
          npx -p @ui5/cli@^3.0.0 ui5 build --dest dist
          --include-task=generateCachebusterInfo
        - bash -c "cd dist && npx bestzip ../../../build/dist/launchpad.zip *"
        - rm -rf dist
      _artifactName: launchpad.zip
      supported-platforms: []
      build-result: ../../build/dist
  - name: OBMS-aux-ui-deployer
    type: com.sap.application.content
    path: build/ui-deployer
    build-parameters:
      build-result: resources
      requires:
        - name: OBMS-customermanagement
          artifacts:
            - customermanagement.zip
          target-path: resources
        - name: OBMS-launchpad
          artifacts:
            - launchpad.zip
          target-path: resources
    requires:
      - name: OBMS-service-html5-apps-repo-host
        parameters:
          content-target: true
resources:
  - type: org.cloudfoundry.managed-service
    name: OBMS-service-uaa
    parameters:
      service: xsuaa
      service-plan: application
      service-name: OBMS-uaa
      path: xs-security.json
      config:
        oauth2-configuration:
          redirect-uris:
            - https://${subdomain}.launchpad.${default-domain}/login/*
            - https://${subdomain}.${default-domain}/login/*
          credential-types:
            - binding-secret
            - x509
        xsappname: OBMS-${space}
        role-collections: []
  - type: org.cloudfoundry.managed-service
    name: OBMS-service-db
    parameters:
      service: hana
      service-plan: hdi-shared
      service-name: OBMS-db
  - type: org.cloudfoundry.managed-service
    name: OBMS-service-destination
    parameters:
      service: destination
      service-plan: lite
      service-name: OBMS-destination
      config:
        HTML5Runtime_enabled: true
        version: 1.0.0
  - type: org.cloudfoundry.user-provided-service
    name: OBMS-service-metadata
    parameters:
      service: metadata
      service-plan: standard
      service-name: OBMS-metadata
      config:
        default-domain: ${default-domain}
  - type: org.cloudfoundry.managed-service
    name: OBMS-service-connectivity
    parameters:
      service: connectivity
      service-plan: lite
      service-name: OBMS-connectivity
  - type: org.cloudfoundry.managed-service
    name: OBMS-service-logs
    parameters:
      service: application-logs
      service-plan: lite
      service-name: OBMS-logs
  - type: org.cloudfoundry.managed-service
    name: OBMS-service-html5-apps-repo-host
    parameters:
      service: html5-apps-repo
      service-plan: app-host
      service-name: OBMS-html5-apps-repo-host
